# Docker Compose Environment Variables
# Copy file này thành .env và điền giá trị thực

# JWT (bắt buộc)
JWT_SECRET_KEY=your-super-secret-key-change-in-production

# MongoDB Atlas Connection String (BẮT BUỘC)
# Lấy từ MongoDB Atlas Dashboard → Connect → Connection String
# Ví dụ: mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/auth_db?retryWrites=true&w=majority
MONGODB_ATLAS_URI=mongodb+srv://your-username:your-password@your-cluster.mongodb.net/auth_db?retryWrites=true&w=majority

# Frontend URL (cho CORS)
FRONTEND_URL=http://localhost:3000
